package main

import (
	"fmt"
)

// Estrutura base de uma funcao
// func (parametros de entrada) tipo de retorno {}
func somar(a int, b int) int {
	return a + b
}

func imprimir(valor int) {
	fmt.Println(valor)
}
